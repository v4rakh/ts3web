{% extends 'layout.twig' %}

{% block content %}
    <h1 class="header">{{ title }}</h1>
    <div class="small">
        {% for log in data %}
            <p>{{ log.l }}</p>
        {% endfor %}
    </div>
{% endblock %}